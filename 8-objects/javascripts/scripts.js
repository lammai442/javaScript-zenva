// Funktion för att logga enklare. Bara att anropa log() och lägga in det som ska loggas som argument.
function log(input) {
    console.log(input);
}

let player = {
    name: 'Lam',
    age: 12,
    isActive: true,
    outfit: {
        color: 'Blue',
        size: 'M',
        isNew: false,
    },
};

player.name;
